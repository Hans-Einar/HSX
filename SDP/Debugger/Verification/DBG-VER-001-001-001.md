# DBG-VER-001-001-001 — DAP Protocol Baseline Verification

- Status: PASS — ACCEPTED BY MASTER SIGN-OFF
- Verification date: 2026-08-19
- Refactor: `DBG-RF-001`
- Slice: `DBG-SL-001-001-001`
- Independent review: `DBG-RVW-001-001-001` — PASS
- Implementation base:
  `e5a50ab45acdcb515ccd3602ce99487bd668cdfd`
- Exact reviewed implementation head:
  `208063e344b767f82790ce579eba6327e2cdd0ce`
- Exact repository head tested:
  `fefd4b0c427dfa71d637e4f4cce9e4a345912591`
- Branch: `codex/dbg-rf-001`

## Verification boundary

This pass independently verified the frozen `DBG-RF-001` / `DBG-SL-001-001-001`
contract against `DBG-F-001..DBG-F-003`, `DBG-R-001`, `DBG-R-002`, `DBG-R-032`, and
the initial evidence obligations for `DBG-R-033` and `DBG-R-036`.

The repository head tested contains the independent review record after the implementation
commit. `git diff --quiet 208063e..fefd4b0 -- . ':(exclude)SDP/**'` passed, proving there is
no product or test diff between the reviewed implementation head and the repository head
used for verification.

No product or test code was changed by the verifier.

## Platform and tools

- Windows `Windows-10-10.0.26200-SP0`, `sys.platform=win32`
- Python 3.11.5 at `C:\Users\hanse\miniconda3\python.exe`
- pytest 8.4.2
- optional `tabulate` 0.9.0 was present during the broad-suite run

## Evidence and results

1. Exact-head and clean-state checks:
   - `git rev-parse HEAD` — PASS,
     `fefd4b0c427dfa71d637e4f4cce9e4a345912591`.
   - `git status --porcelain` — PASS, empty before verification records were written.
   - `git diff --quiet 208063e..fefd4b0 -- . ':(exclude)SDP/**'` — PASS; no product/test
     change after independent review.
2. Targeted contracted suite:
   - `c:/Users/hanse/miniconda3/python.exe -m pytest python/tests/test_hsx_dap_cli.py python/tests/test_hsx_dap_harness.py python/tests/test_hsx_dbg_backend.py -q`
   - PASS, `36 passed in 0.31s`.
3. Production-wrapper lifecycle and framing scenarios:
   - `c:/Users/hanse/miniconda3/python.exe -m pytest python/tests/test_hsx_dap_cli.py -vv`
   - PASS, `3 passed in 0.24s`.
   - The three collected cases explicitly covered strict preamble rejection and initialize
     plus launch and attach through `vscode-hsx/debugAdapter/hsx-dap.py`. Both session cases
     observed the successful initialize response before the `initialized` event.
4. Strict byte-boundary negative controls:
   - A direct invocation of `_assert_only_framed_messages` supplied one valid DAP frame with
     an injected raw preamble and then with trailing raw bytes.
   - PASS, `2/2 rejected`: both inputs raised `AssertionError` for unframed stdout bytes.
5. Syntax and import sanity:
   - `c:/Users/hanse/miniconda3/python.exe -m py_compile python/hsx_dap/__init__.py python/tests/dap_stubs.py python/tests/test_hsx_dap_cli.py vscode-hsx/debugAdapter/hsx-dap.py`
     — PASS.
   - With the project's `python` directory on `PYTHONPATH`, importing `hsx_dap` and
     `tests.dap_stubs` — PASS.
   - An initial ad hoc probe using `import python.hsx_dap` without the project `PYTHONPATH`
     failed to find top-level `hsx_dbg`; this was a probe configuration error, not the
     production-wrapper launch configuration or a Slice-owned failure.
6. Broad Python regression suite:
   - `c:/Users/hanse/miniconda3/python.exe -m pytest python/tests -q`
   - Nonzero, `534 passed, 2 skipped, 2 failed in 5.34s`.
   - `test_break_add_symbol_line` failed because ignored generated artifact
     `examples/demos/build/debug/longrun/main.sym` is absent. It is absent from both the base
     and implementation Git trees, and neither its owning test nor demo/build inputs changed
     in the Slice.
   - `test_pretty_dmesg_assigns_session_numbers` failed because the untouched optional
     `tabulate` rendering path formats the session number as a table cell while the assertion
     expects fallback text containing `session=1`. Neither `python/shell_client.py` nor
     `python/tests/test_shell_client.py` changed in the Slice.
   - Both failures are classified as pre-existing/non-Slice evidence. No out-of-scope fix was
     made.
7. Traceability validation:
   - Parsed `CurrentIndex.yaml`, `Issues.yaml`, and `Relations.yaml` with `yaml.safe_load` —
     PASS.
   - Parsed every nonblank `Ledger.ndjson` line with `json.loads` — PASS, 13 pre-verification
     events.
8. Exact diff and scope checks:
   - `git diff --check e5a50ab..fefd4b0` — PASS, no output.
   - The implementation diff from `e5a50ab..208063e` contains only the owning Debugger SDP
     records plus `python/hsx_dap/__init__.py`, `python/tests/dap_stubs.py`,
     `python/tests/test_hsx_dap_cli.py`, and `vscode-hsx/debugAdapter/hsx-dap.py`.
   - The small `dap_stubs.py` change remains within the frozen directly related fixture
     allowance documented by the independent reviewer.

## Requirement result

- `DBG-R-001` / `DBG-F-001`: PASS on Windows. The production wrapper emitted only parseable
  framed DAP stdout in the exercised sessions, and both raw-byte negative controls failed
  closed.
- `DBG-R-002` / `DBG-F-002`: PASS. The initialize response was observed before
  `initialized` through the real wrapper.
- `DBG-R-032` / `DBG-F-003`: PASS. Automated subprocess coverage launched the same wrapper
  selected by the VS Code extension and exercised initialize, launch, and attach.
- `DBG-R-033`: partial cross-platform evidence only. Windows PASS is recorded here; Linux is
  not claimed.
- `DBG-R-036`: PASS for this Slice's initial regression-oracle obligation. Strict framing and
  production-entrypoint behavior are captured before structural replacement.

## Linux probe and residuals

WSL2/openSUSE Leap 15.5 was reachable, but its available runtime was Python 3.6.15 and
`python3 -m pytest --version` failed with `No module named pytest`. No suitable Linux project
test environment was available, no environment was mutated to manufacture one, and no Linux
PASS is claimed. The Linux product-wrapper evidence remains assigned to `DBG-RF-009` under
the frozen contract.

The two classified broad-suite failures remain non-Slice repository evidence. They do not
block the narrow `DBG-RF-001` completion signal and are not silently fixed here.

## Verification decision

**PASS for `DBG-VER-001-001-001`.** The reviewed Slice behavior passed the contracted Windows
evidence, review `DBG-RVW-001-001-001` is PASS with no findings, and no Slice-owned failure
was observed. This verification was initially recorded as `verified_pending_master_signoff`
and was subsequently accepted by Master reconciliation.

Master reconciliation accepted this record for exact implementation head
`208063e344b767f82790ce579eba6327e2cdd0ce`. `DBG-DA-001` / issue #38 is now the next
separate gate but is not started. Structural `DBG-RF-002..DBG-RF-009` product work remains
blocked until the DesignAnalysis and relevant `DBG-D-*` contracts are accepted.
