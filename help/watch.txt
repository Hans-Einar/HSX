watch <subcommand>

Subcommands:
  add <pid> <expr> [--type address|symbol|local] [--length N]
  remove <pid> <watch_id>
  list <pid>

Adds, removes, or lists watch expressions via the executive `watch` RPC. Watch
updates appear in the event stream when values change.
